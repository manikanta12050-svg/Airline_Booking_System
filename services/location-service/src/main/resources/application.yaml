server:
  port: 5004

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/locationdb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect